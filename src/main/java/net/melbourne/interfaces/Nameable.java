package net.melbourne.interfaces;

/**
 * Should be used for anything with a name.
 * Most things in this client shouldn't have changing names, other than Modules.
 */
public interface Nameable {
    String getName();
}
